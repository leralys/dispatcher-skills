import styled from 'styled-components';
import { FlexColumn } from '../../../../styles/sharedStyles';

export const GraphsContainer = styled(FlexColumn)`
  width: 28.6%;
  height: 100%;
  gap: 1.5rem;
`;
